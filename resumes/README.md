# Viewing

To view a `.docx` file, click on it and then click on `View Raw` on the page that appears.

To view a `.pdf` file, click on it and it should render in the browser. If not, click on `View Raw` as above.
